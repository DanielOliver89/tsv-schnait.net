prototype(tsv.schnait:YouTube) < prototype(Neos.Neos:ContentComponent) {
    videoId = ${q(node).property('video')}
    inBackend = ${node.context.inBackend}

    renderer = afx`<tsv.schnait:Presentation.YouTube {...props} />`
}
